{% extends 'base.html.twig' %}

{% block title %}Article --
	{{ article.title }}
{% endblock %}

{% block body %}


	<div class="">
		<h1>{{article.title}}</h1>
		<p>{{article.content}}</p>
		<small>Slug:
			{{article.slug}}</small>
	</div>
{% endblock %}
